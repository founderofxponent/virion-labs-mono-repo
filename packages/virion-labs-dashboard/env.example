# Virion Labs Dashboard Environment Variables
# Copy this file to .env.local and fill in your actual values

# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (Required for admin scripts)
# This key has admin privileges - keep it secure!
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Optional: Database URL (for direct database operations)
DATABASE_URL=your_database_url_here

# Application URLs (Required)
# Base URL of your application (used for referral links and redirects)
# In development: http://localhost:3000
# In production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Referral Link Domain (Optional)
# If you have a custom domain for referral links (e.g., ref.yourdomain.com)
# Leave empty to use the main app URL for referral links
NEXT_PUBLIC_REFERRAL_DOMAIN=

# Optional: Discord Bot Configuration
DISCORD_BOT_TOKEN=your_discord_bot_token
DISCORD_CLIENT_ID=your_discord_client_id_here

# Discord Bot Webhook URL (Optional - for immediate cache invalidation)
# URL where the Discord bot can receive cache invalidation requests
# If not set, cache will rely on TTL expiration (30 seconds)
DISCORD_BOT_WEBHOOK_URL=http://localhost:3001/webhook

# Discord Bot API Key (Optional)
DISCORD_BOT_API_KEY=your_bot_api_key

# Discord Bot Internal URL (Optional)
DISCORD_BOT_INTERNAL_URL=http://localhost:3001

# Discord Server Configuration
DISCORD_GUILD_ID=
DISCORD_JOIN_CAMPAIGNS_CHANNEL_ID=

# Optional: External API Keys
# Add any other API keys your application needs here

# Notes:
# 1. Never commit .env.local to version control
# 2. The service role key should be kept secure and rotated regularly
# 3. You can find these values in your Supabase project dashboard
# 4. Set NEXT_PUBLIC_APP_URL to your actual domain in production
# 5. Remove this file after creating your .env.local 